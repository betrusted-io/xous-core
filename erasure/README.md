# Erasure

It's an 80's pop music band!

It's also an experimental baremetal build to quantify, measure and validate that an exact amount of memory is in the system, and that the memory is in a known state.
