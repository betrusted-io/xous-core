[workspace]
members = [
    "kernel",
    "loader",
    "tools",
    "xous-rs",
    "examples/shell",
]
default-members = ["kernel", "loader", "xous-rs", "examples/shell"]

[profile.release]
codegen-units = 1 # 1 better optimizations
debug = true # symbols are nice and they don't increase the size on Flash
lto = true
#opt-level=0
